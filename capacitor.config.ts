import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pawmood.app',
  appName: 'PawMood',
  webDir: 'dist',
  // server: {
  //   url: 'http://192.168.x.x:8080', // Uncomment for live reload during dev
  //   cleartext: true
  // }
};

export default config;
